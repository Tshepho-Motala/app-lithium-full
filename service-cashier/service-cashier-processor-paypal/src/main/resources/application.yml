server.port: 9623

ribbon:
  ReadTimeout: 30000
  ConnectTimeout: 30000
  
spring:
  application:
    name: service-cashier-processor-paypal
  cloud:
    stream:
      bindings:
        processorsregisteroutput:
          group: processorsregistergroup
          binder: rabbit
          destination: processorsregisterqueue
          producer:
            required-groups: processorsregistergroup
  thymeleaf:
    cache: false

logging:
  level:
    lithium.service.cashier: INFO
    lithium.service.cashier.processor.http: INFO
    lithium.service.cashier.processor.paypal: INFO
